{
  "name": "jcode-ext-git-scm",
  "version": "0.3.0",
  "private": true,
  "description": "Source Control extension for JCode — a native Compose plugin drawn inside JCode's own process.",
  "scripts": {
    "build": "node build.mjs"
  }
}
