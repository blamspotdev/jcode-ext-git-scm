{
  "name": "jcode-ext-git-scm",
  "version": "0.3.0",
  "private": true,
  "description": "Source Control extension for JCode — built with esbuild + TypeScript.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.5.4"
  }
}
